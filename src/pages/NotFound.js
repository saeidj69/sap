

import MainPage from "../components/hoc/mainPage";
import NotFoundComponent from "../components/not-found";

const Home = () => {
  

  return (
    <MainPage>
      <NotFoundComponent />
    </MainPage>
  );
};

export default Home;
